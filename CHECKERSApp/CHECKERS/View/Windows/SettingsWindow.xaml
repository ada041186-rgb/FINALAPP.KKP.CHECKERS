<Window x:Class="CHECKERS.View.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Налаштування" Height="220" Width="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#1A0F0A">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0"
                   Text="Роздільна здатність:"
                   Foreground="#F0E6D3" FontSize="14"
                   VerticalAlignment="Center" Margin="0,0,0,12"/>

        <ComboBox Grid.Row="0" Grid.Column="1"
                  ItemsSource="{Binding Resolutions}"
                  SelectedItem="{Binding SelectedResolution}"
                  Background="#2D1810" Foreground="#2D1810"
                  BorderBrush="#8B4513"
                  Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="Повний екран:"
                   Foreground="#F0E6D3" FontSize="14"
                   VerticalAlignment="Center"/>

        <CheckBox Grid.Row="1" Grid.Column="1"
                  IsChecked="{Binding IsFullScreen}"
                  Foreground="White" VerticalAlignment="Center"
                  Margin="4,0,0,0"/>

        <Separator Grid.Row="3" Grid.ColumnSpan="2"
                   Background="#8B4513"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button Content="Зберегти"
                    Command="{Binding SaveCommand}"
                    Width="90" Height="30" Margin="0,0,10,0"
                    Background="#8B4513" Foreground="White"
                    BorderThickness="0" Cursor="Hand"/>

            <Button Content="Скасувати"
                    Command="{Binding CancelCommand}"
                    Width="90" Height="30"
                    Background="#4A2010" Foreground="White"
                    BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>